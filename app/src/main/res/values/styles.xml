<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
    <!--<style name="AppTheme" parent="Theme.AppCompat.Dialog.Alert">-->
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/colorText</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
        <!--<item name="android:actionButtonStyle">@style/AppTheme.PaddedActionIcons</item>-->
    </style>

    <!--Possibly remove this, trying to arrange icons with a Toolbar-->
    <style name="AppTheme.PaddedActionIcons" parent="Widget.AppCompat.ActionButton">
        <item name="android:minWidth">150dp</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="Base.ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="clockface_text">
        <item name="android:textAlignment">center</item>
        <item name="android:textSize">100dp</item>
    </style>

    <style name="title_text">
        <item name="android:textSize">40sp</item>
        <item name="android:textAlignment">center</item>
    </style>

    <style name="large_text">
        <item name="android:textSize">20sp</item>
    </style>

    <style name="small_text">
        <item name="android:textSize">14sp</item>
    </style>
</resources>
